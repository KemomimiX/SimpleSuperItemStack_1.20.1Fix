name: Build SSIS 1.20.1

on:
  push:
    branches:
      - 1.20.1-arch
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: 1.20.1

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Grant execute permission
        run: chmod +x gradlew

      - name: Build Forge Jar
        run: ./gradlew :forge:jar --no-daemon

      - name: Find generated JARs
        run: |
          echo "================================"
          echo "Generated JAR files:"
          echo "================================"
          find . -type f -name "*.jar" -print

          echo ""
          echo "================================"
          echo "Forge build directory:"
          echo "================================"
          find forge/build -type f 2>/dev/null || true

          echo ""
          echo "================================"
          echo "All build directories:"
          echo "================================"
          find . -type d -name build -print

      - name: Upload Forge JAR
        uses: actions/upload-artifact@v4
        with:
          name: ssis-forge-1.20.1
          path: |
            **/build/libs/*.jar
          if-no-files-found: error
